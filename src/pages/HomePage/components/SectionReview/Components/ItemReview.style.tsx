import { Box, styled } from '@mui/material';

export const ThumbReview = styled(Box)(({ theme }) => ({
  width: '103px',
  height: '103px',
  borderRadius: '103px',
  overflow: 'hidden',
  marginBottom: '15px',
  display: 'inline-block'
}));
